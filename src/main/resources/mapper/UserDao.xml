<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserDao">

	<insert id="addCount">
		INSERT INTO user (userid, name, phone, password) values (#{userid},"",#{phone},#{password})
	</insert>

	<update id="changeImg">
		UPDATE user
		SET user_photo=#{user_photo}
		WHERE userid=#{userid}
	</update>

    <select id="queryUser" resultType="User">
        SELECT userid
        FROM user
        WHERE
        phone =#{phone}
    </select>
	
	<select id="selecPasswordByphone" resultType="User">
        SELECT password
        FROM user
        WHERE
        phone =#{phone}
    </select>

	<select id="queryByPhone" resultType="User">
		SELECT *
		FROM _user
		WHERE 
		user_phone= #{userPhone} limit 1
	</select>
	
	<select id="queryAll" resultType="User">
		SELECT *
		FROM _user
		ORDER BY create_time DESC
		limit #{offset},#{limit}
	</select>


   	<!--<update id="addScore">-->
		<!--UPDATE _user -->
		<!--SET -->
		<!--score = score + #{add}-->
	<!--</update>-->

</mapper>